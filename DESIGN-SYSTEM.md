# دليل تنظيم الموارد التصميمية — مشروع موقع عوالم

هذا الملف هو **مصدر الحقيقة الوحيد** لكل ما يخص الشعارات والخطوط والألوان في المشروع.
أي قرار تصميمي (لون جديد، خط جديد، نسخة شعار جديدة) يُوثَّق هنا أولًا، ثم يُطبَّق في الكود.

آخر تحديث: بعد استلام محتوى الموقع الحقيقي (لقطات شاشة من awalim.om) وإعادة بناء الصفحة الرئيسية + أربع صفحات مجالات مستقلة على أساسه.

---

## 1. الهدف

- الوصول لأي مورد تصميمي (شعار، خط، لون) بثوانٍ بدل البحث بين الملفات.
- إعادة استخدام نفس الموارد في كل صفحة/مكوّن بدون تكرار أو نسخ-لصق.
- ضمان اتساق الهوية البصرية حتى لو المشروع كبر لصفحات متعددة.

---

## 2. خريطة الموقع الفعلية

```
index.html          ← الرئيسية: الهيرو + عن عوالم (رسالتنا/رؤيتنا) + مجالات عملنا + إحصائيات
education.html       ← مجال: تعليم
writing.html          ← مجال: كتابة
media.html             ← مجال: إعلام
consulting.html         ← مجال: مشورة
```

كل صفحة مجال فيها 3 أقسام فرعية (إيماني / ثقافي / علمي) كهيكل جاهز — محتواها التفصيلي غير موجود بعد (بانتظار تفصيل لاحق)، ومُعلَّم بوضوح "قريبًا" داخل الصفحة نفسها حتى ما يُقرأ كمحتوى نهائي.

**مصدر المحتوى:** كل نصوص الهيرو، عن عوالم، مجالات عملنا، والإحصائيات مأخوذة **حرفيًا** من لقطات شاشة فعلية للموقع الحي awalim.om (وليس من الكتيّب PDF القديم، ولا من أي صياغة من عندنا) — بما في ذلك رسالة عوالم الرسمية التي ما كانت متوفرة قبل هذي اللقطات.

---

## 3. بنية المجلدات الفعلية

```
awalim-website/
├── index.html
├── education.html
├── writing.html
├── media.html
├── consulting.html
├── DESIGN-SYSTEM.md        ← هذا الملف
│
├── assets/
│   ├── logo/
│   │   ├── awalim-logo-full-master.png   ← أعلى دقة (3211×2758) — مصدر أصلي، لا يُستخدم مباشرة بالويب
│   │   ├── awalim-logo-full.png          ← نسخة مُحسّنة للويب (1200px) — الشعار كامل (رمز + الاسمان عربي/إنجليزي)
│   │   ├── awalim-mark-master.png        ← أعلى دقة للرمز فقط (بدون الاسم)
│   │   ├── awalim-mark.png               ← نسخة مُحسّنة للويب (600px) — مستخدمة حاليًا بالناف بار
│   │   └── favicon/
│   │       ├── favicon.ico
│   │       ├── favicon-32.png
│   │       ├── favicon-192.png
│   │       └── apple-touch-icon.png
│   │
│   └── fonts/
│       └── manchette/
│           ├── Manchette-Thin.woff2
│           ├── Manchette-Regular.woff2
│           ├── Manchette-Bold.woff2
│           └── manchette-fonts.css        ← تعريفات @font-face، بجانب ملفات الخط مباشرة
│
├── css/
│   ├── tokens.css       ← الألوان + المسافات (متغيرات CSS فقط، بلا تصميم)
│   ├── base.css         ← إعادة الضبط + الطباعة الأساسية + تبديل اللغة
│   ├── components.css   ← الناف بار + المخطط الهرمي (mega-menu)، الفقاعة، الأزرار، الفوتر — يتكرر بكل صفحة
│   ├── home.css         ← تخطيط خاص بـ index.html فقط (الهيرو، عن عوالم، بطاقات المجالات، الإحصائيات)
│   └── field.css         ← تخطيط خاص بصفحات المجالات الأربع فقط (page-intro + بطاقات الجوانب الثلاثة)
│
└── js/
    └── main.js           ← تبديل اللغة، عدّاد الإحصائيات، قائمة الجوال، فتح/إغلاق المخطط الهرمي
```

**ملاحظة على القرار التنظيمي:** فصلنا `home.css` عن `field.css` عن `components.css` بحسب مين يستخدم كل قسم فعليًا — مو حسب "كل شيء بملف وحد". أي CSS يظهر بصفحة وحدة بس (الهيرو مثلًا) يروح لملف خاص بتلك الصفحة، وأي شيء يتكرر بكل الصفحات (الناف بار، الفوتر، الأزرار) يبقى بـ`components.css`.

**لماذا هذا التقسيم مهم:** أي صفحة مجال جديدة (لو احتجنا نضيف مجال خامس مستقبلًا) تحتاج بس تربط نفس الروابط (`manchette-fonts.css`, `tokens.css`, `base.css`, `components.css`, `field.css`) وتاخذ كل الهوية والتخطيط جاهزين.

---

## 4. الشعار — القواعد

**الوضع الحالي:** الشعار المُستلم ملف **PNG بخلفية شفافة** بدقة عالية جدًا (4501×5625 أصليًا)، مو ملف متجهي (SVG). استُخدم **كما هو تمامًا بدون أي تعديل على الألوان أو الأشكال** — فقط قصّ الهوامش الفارغة الزائدة (إجراء تقني بحت، ما يُعتبر تعديلًا على التصميم).

| الملف | متى يُستخدم |

|---|---|
| `awalim-mark.png` | الناف بار، الفافيكون، أي مساحة ضيقة — **مستخدَم فعليًا الآن** |
| `awalim-logo-full.png` | أي مكان يحتاج الشعار كامل (رمز + الاسم) **على خلفية فاتحة/كريمية فقط** |

> ⚠️ **قيد حقيقي مهم:** نص الشعار (`مؤسسة عوالم` / `Awalim Institution`) مطبوع بلون أخضر غامق (`#12493E`) ضمن ملف الصورة نفسه. هذا اللون قريب جدًا من خلفية قسم الهيرو والفوتر التركوازية الغامقة (`--teal-900`) — فوضع `awalim-logo-full.png` على تلك الخلفيات يخلي النص شبه مختفٍ. **لذلك لا يُستخدم إلا على خلفية فاتحة.** لو احتجتِ الشعار الكامل على خلفية غامقة مستقبلًا، الحل الصحيح هو طلب نسخة رسمية إضافية بألوان معكوسة من نفس الجهة اللي صمّمت الشعار — مو تعديلها بنفسنا.

**قواعد عامة لا تزال تنطبق:**
- **مساحة فارغة حول الشعار (Clear space):** لا يقل عن ارتفاع الفقاعة الواحدة من كل جهة.
- **الحجم الأدنى:** 28px ارتفاعًا على الشاشة.
- لو تحصّلتِ يومًا على نسخة **SVG متجهية** من نفس الشعار (بدل PNG)، هذا يستاهل يحل محل ملفات PNG الحالية — يكبر بلا فقدان جودة وحجمه أصغر. اطلبيها من نفس مصمم الهوية لو متوفرة.

---

## 5. الخط

**الوضع الحالي:** المشروع يستخدم خط **Manchette** الرسمي — خط واحد بس، يدعم العربي والإنجليزي معًا بنفس الملف (تأكدنا إنه يحتوي جداول تشكيل عربي كاملة). استُبدلت به كل الخطوط المؤقتة المستخدمة بالنموذج الأول (El Messiri / Readex Pro / Space Grotesk / Instrument Sans).

| الوزن | ملف الخط | الاستخدام |
|---|---|---|
| Thin (100) | `Manchette-Thin.woff2` | غير مستخدَم حاليًا بالكود — متاح لعناصر زخرفية كبيرة مستقبلًا |
| Regular (400) | `Manchette-Regular.woff2` | كل النصوص العادية (فقرات، تسميات، روابط) |
| Bold (700) | `Manchette-Bold.woff2` | كل العناوين والتشديد (الأزرار، أرقام الإحصائيات، الشعار النصي) |

الخط محمَّل محليًا بالكامل (مو من رابط خارجي) عبر `assets/fonts/manchette/manchette-fonts.css` — يعني الموقع يشتغل حتى بدون اتصال إنترنت، وما يعتمد على خادم Google أو أي طرف ثالث.

**قاعدة:** أي وزن جديد تحتاجينه (مثلًا Medium) لازم يُطلب كملف OTF/TTF رسمي من نفس مصدر الخط — لا يُصنَع صناعيًا بـ`font-weight` بينية لأن المتصفح "يزيّفه" (synthetic bold) وتطلع النتيجة رديئة.

---

## 6. الألوان

القيم الحالية **مسحوبة فعليًا من بكسلات ملف الشعار ومن غلاف الكتيّب الرسمي** (PDF) — مو تقدير بالعين. كل الألوان مُعرَّفة كمتغيرات CSS في `tokens.css`.

```css
:root{
  /* التركوازي — من غلاف الكتيّب الرسمي مباشرة */
  --teal-900:#153B3A;   /* خلفيات غامقة (الهيرو، الفوتر) — أغمق مُشتق */
  --teal-700:#266C6A;   /* اللون الأساسي — القيمة الحقيقية من الغلاف */
  --teal-600:#2D7F7D;   /* حالة hover — أفتح مُشتق */

  /* الذهبي — مسحوب من فقاعة الشعار اليمنى */
  --gold-500:#FBD048;
  --gold-600:#FAC009;   /* حالة hover — أغمق مُشتق */

  /* الأخضر السيج — مسحوب من فقاعة الشعار اليسرى */
  --sage-400:#D9DD55;
  --sage-600:#A9AD23;   /* نص/تمييز على خلفية فاتحة — أغمق مُشتق */

  /* الكريمي — الخلفية */
  --cream-050:#FFFFFF;
  --cream-100:#FBF7EE;

  /* الحبر — مسحوب من نص "مؤسسة عوالم" بالشعار نفسه */
  --ink-900:#12493E;    /* النص الأساسي */
  --ink-700:#4A7368;    /* نص ثانوي — ممزوج مع الكريمي، تباين 4.98:1 */
  --ink-400:#749288;    /* نص خافت/زخرفي فقط — تباين 3.16:1 (لا يُستخدم لنص أساسي صغير) */
}
```

كل قيم الـ`hover` والدرجات الثانوية **مُشتقة رياضيًا** من القيم الحقيقية (تفتيح/تعتيم أو مزج مع الكريمي) — مو اختيار عشوائي، وكل قيمة نص تم التأكد من نسبة تباينها (WCAG) قبل اعتمادها.

**قاعدة صارمة:** لا يُكتب أي كود hex (`#266C6A`) مباشرة داخل أي ملف CSS آخر أو داخل HTML. يُستخدم دائمًا المتغيّر (`var(--teal-700)`).

---

## 7. أفضل الممارسات العامة

- **مصدر واحد للحقيقة:** كل صفحة HTML تستدعي نفس ملفات الهوية المشتركة (`manchette-fonts.css`, `tokens.css`, `base.css`, `components.css`) بالإضافة لملف التخطيط الخاص بها (`home.css` أو `field.css`) — بدون تكرار أي كود.
- **تسمية الملفات بالإنجليزية دائمًا** (kebab-case) حتى لو محتوى الموقع عربي بالكامل.
- **Git من أول يوم:** `git init` بمجلد المشروع من البداية يحفظ تاريخ كل تعديل.
- **تحديث هذا الملف أولًا:** أي قرار جديد يُكتب هنا قبل ما يُطبَّق بالكود.
